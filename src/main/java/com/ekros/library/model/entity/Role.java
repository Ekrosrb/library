package com.ekros.library.model.entity;

import java.io.Serializable;
/**
 * Role - enum with the roles that exist on the site
 * @author ekros
 * */
public enum Role implements Serializable {
    ADMIN, LIBRARIAN, USER
}
