package com.ekros.library.model.entity;
/**
 * Status - enum with possible statuses of user orders
 * @author ekros
 * */
public enum Status {
    ACCEPTED,
    REJECTED,
    ON_USE,
    EXPIRED,
    CLOSED,
    PENDING,
    PAID
}
